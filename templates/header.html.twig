<nav class="navbar fixed-top navbar-light bg-light nav-top">
    <div class="container">
        <a href="{{ path('index') }}">
            <img src="" alt="logo">
        </a>
        <a class="navbar-brand" href="{{ path('authorization_sign_in') }}">Sign in</a>
    </div>
</nav>
